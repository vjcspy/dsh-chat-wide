/**
 * Locale entry point for the transcript-width Settings row.
 *
 * The dictionaries live in `./locales/en.ts`; this module is the import site the
 * row and its tests use, so the file layout stays free to grow a locale per file
 * without touching a caller.
 */

export { bind, en, zh, type LocaleKey, type Translate } from './locales/en.ts'
